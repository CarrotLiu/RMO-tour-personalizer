import{motion,useScroll,useTransform}from'framer-motion';import{Challenge}from'../data/challenges';import{JournalCard}from'../components/JournalCard';
export function HomePage({unlocked,onStart,onCollection}:{unlocked:Challenge[];onStart:()=>void;onCollection:()=>void}){const{scrollY}=useScroll();const introOpacity=useTransform(scrollY,[0,120],[1,.28]);return <div className='home page-scroll'><motion.div className='intro-card' style={{opacity:introOpacity}}><p>Explore the exhibition, discover artifacts, and unlock pages of your field journal as you complete museum challenges.</p><div className='paper-tilt'>☠</div></motion.div><button className='primary-button' onClick={onStart}>Solve Your Challenges Now <span>›››</span></button><div className='section-title'><span>Your Journal Pages</span><button onClick={onCollection}>See all ›</button></div><div className='horizontal-list'>{unlocked.map(i=><JournalCard key={i.id} challenge={i}/>) }<div className='locked-card'>?</div></div><div className='how-it-works'><h2>How it works</h2><ol><li>Find artifacts around the exhibition.</li><li>Solve the challenge connected to each object.</li><li>Unlock your page collection.</li></ol></div></div>}
