import{Challenge}from'../data/challenges';import{JournalCard}from'../components/JournalCard';
export function CollectionPage({unlocked,allChallenges}:{unlocked:Challenge[];allChallenges:Challenge[]}){return <div className='collection page-scroll'><div className='collection-banner'><strong>{unlocked.length} pages completed</strong><span>{allChallenges.length-unlocked.length} pages to unlock</span></div><div className='grid'>{allChallenges.map(c=>unlocked.some(i=>i.id===c.id)?<JournalCard key={c.id} challenge={c}/>:<div className='collection-lock' key={c.id}>🔒</div>)}</div></div>}
