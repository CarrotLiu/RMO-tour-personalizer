import{useState}from'react';import{motion}from'framer-motion';export function FlipOption({label,backText}:{label:string;backText:string}){const[flipped,setFlipped]=useState(false);return <button className='flip-shell' onClick={()=>setFlipped(v=>!v)}><motion.div className='flip-card' animate={{rotateY:flipped?180:0}}><div className='flip-face front'>{label}</div><div className='flip-face back'>{backText}</div></motion.div></button>}
