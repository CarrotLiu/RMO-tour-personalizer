name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: museum-management/package-lock.json

      - name: Install museum-management dependencies
        working-directory: museum-management
        run: npm ci

      - name: Build museum-management
        working-directory: museum-management
        run: npm run build

      - name: Prepare Pages artifact
        run: |
          mkdir _site
          rsync -a \
            --exclude='.git/' \
            --exclude='.github/' \
            --exclude='**/node_modules/' \
            --exclude='museum-management/dist/' \
            ./ _site/
          rm -rf _site/museum-management
          mkdir -p _site/museum-management
          cp -R museum-management/dist/. _site/museum-management/
          touch _site/.nojekyll

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
